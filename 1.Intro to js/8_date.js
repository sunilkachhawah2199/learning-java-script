console.log(new Date(74654560).toString());  // dates are stored in milliseconds
// javascript 's proposal is to use Temporal API, but it is not supported in all browsers yet

// 1. create a date object
const date = new Date();
console.log(date);


